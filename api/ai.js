export default async function handler(req,res){if(req.method!=='POST'){res.status(405).json({error:'Method not allowed'});return}try{const{message,context}=req.body||{};if(!message||typeof message!=='string'){res.status(400).json({error:'A message is required.'});return}const apiKey=process.env.OPENAI_API_KEY;if(!apiKey){res.status(503).json({error:'AI service is not configured yet.'});return}const safeContext=typeof context==='string'?context.slice(0,12000):'';const system=`You are Sharova, a calm and practical personal operating system assistant. Help the user prioritize tasks, deadlines, documents, career, money, travel and home life. Give concise, actionable answers. Never invent data. Use supplied workspace context when relevant. Do not reveal secrets or API keys.\n\nWorkspace context:\n${safeContext}`;const response=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${apiKey}`},body:JSON.stringify({model:process.env.OPENAI_MODEL||'gpt-5-mini',instructions:system,input:message,max_output_tokens:500})});const data=await response.json();if(!response.ok){res.status(response.status).json({error:data?.error?.message||'AI request failed.'});return}const text=data.output_text||(data.output||[]).flatMap(i=>i.content||[]).map(p=>p.text||'').filter(Boolean).join('\n')||'I could not generate a response.';res.status(200).json({text})}catch(error){res.status(500).json({error:'The AI service is temporarily unavailable.'})}}